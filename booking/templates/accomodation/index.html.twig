{% extends 'base.html.twig' %}

{% block title %}Hello AccomodationController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
{% endif %}
<div class="example-wrapper">
    <a href="{{path("accomodation_create", {type:'Boat'})}}">
    <button type="button" class="btn btn-secondary">Boat</button></a>
     <a href="{{path("accomodation_create", {type:'Apartement'})}}"><button type="button" class="btn btn-secondary">Apartment</button></a>
      <a href="{{path("accomodation_create", {type:'House'})}}"><button type="button" class="btn btn-secondary">House</button></a>
       <a href="{{path("accomodation_create", {type:'TreeHouse'})}}"><button type="button" class="btn btn-secondary">TreeHouse</button></a>
      

</div>
{% endblock %}
